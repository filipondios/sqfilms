<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/static/css/common.css">
    <link rel="stylesheet" href="/static/css/new.css">
    <link rel="icon" href="/static/img/icon.svg" type="image/svg+xml">
    <script src="/static/js/edit-review.js" defer></script>
</head>
<body>
<header class="page-header">
    <div class="header-top">
        <h1>{{ title }}</h1>
    </div>
</header>

<div class="main-content">
    <form id="review-form" class="review-form" data-id="{{ review.id }}">
        <label>
            Title:
            <input type="text" name="title" required value="{{ review.title }}">
        </label>
        <label>
            Note (0-10):
            <input type="number" name="note" min="0" max="10" step="0.1" required value="{{ review.note }}">
        </label>
        <label>
            Date:
            <input type="date" name="date" value="{{ review.date }}">
        </label>

        {% if review.season is defined and review.season %}
        <div id="season-field" class="season-field">
            <label>
                Season number:
                <input type="number" name="season" min="1" value="{{ review.season }}">
            </label>
        </div>
        {% endif %}

        <div class="form-buttons">
            <button type="submit" class="btn-primary">Save Changes</button>
            <a href="/" class="btn-cancel">Cancel</a>
        </div>
    </form>
</div>
</body>
</html>